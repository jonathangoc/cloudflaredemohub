{
  "name": "cloudflare-demo-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:aichatbot": "npm run dev --prefix AiChatBot",
    "build:frontend": "npm run build --prefix frontend",
    "deploy:frontend": "npm run deploy --prefix frontend",
    "deploy:aichatbot": "npm run deploy --prefix AiChatBot",
    "install:all": "npm install --prefix frontend && npm install --prefix AiChatBot"
  }
}
